spring:
  application:
    name: google-cloud-storage
  cloud:
    gcp:
      project-id: lofty-root-378503

management:
  endpoints:
    web:
      exposure:
        include: health, info

gcp:
  storage:
    bucket:
      name: jm_lofty-root-cmek-test
